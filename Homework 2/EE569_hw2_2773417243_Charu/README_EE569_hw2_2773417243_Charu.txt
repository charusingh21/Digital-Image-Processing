# EE569 Homework Assignment #2
# Date: Feb 16, 2020
# Name: Charu Singh
# ID: 2773-4172-43
# email: charusin@usc.edu
#
# Software: Visual Studio 
========================================================================
    CONSOLE APPLICATION : [SOBEL] Project Overview
========================================================================

This file contains a summary of what you will find in each of the files that
make up your [Sobel Edge Detection] application.

Steps:

1. Open sobel1.cpp.
	This is the main program for the algorithm.
2. All the header files and the source files have already been included in the project. 
3. Run the program and follow the instructions. 
4. Build the binary for the cpp using following commands:
    g++ -std=c++11 sobel1.cpp
    ./a.out
5. output will be saved as sobel1.raw
6. output will be saved as sobel2.raw
7.output will be saved as sobel3.raw
8.output will be saved as sobel4.raw




 CONSOLE APPLICATION : [Fixed thresholding] Project Overview
========================================================================

This file contains a summary of what you will find in each of the files that
make up your [Fixed] application.

Steps:

1. Open dithering_fixed.cpp.
	This is the main program for the algorithm.
2. All the header files and the source files have already been included in the project. 
3. Run the program and follow the instructions. 
4. Build the binary for the cpp using following commands:
    g++ -std=c++11 dithering_fixed.cpp
    ./a.out
5. output will be saved as fixed.raw


CONSOLE APPLICATION : [Random thresholding] Project Overview
========================================================================

This file contains a summary of what you will find in each of the files that
make up your [random thresholding] application.

Steps:

1. Open dithering_random.cpp.
	This is the main program for the algorithm.
2. All the header files and the source files have already been included in the project. 
3. Run the program and follow the instructions. 
4. Build the binary for the cpp using following commands:
    g++ -std=c++11 dithering_random.cpp
    ./a.out
5. output will be saved as random.raw


CONSOLE APPLICATION : [Dithering] Project Overview
========================================================================

This file contains a summary of what you will find in each of the files that
make up your [Dithering] application.

Steps:

1. Open dithering_matrix.cpp.
	This is the main program for the algorithm.
2. All the header files and the source files have already been included in the project. 
3. Run the program and follow the instructions. 
4. Build the binary for the cpp using following commands:
    g++ -std=c++11 dithering_matrix.cpp
    ./a.out
5. output will be saved as dither_2.raw


CONSOLE APPLICATION : [Dithering_8] Project Overview
========================================================================

This file contains a summary of what you will find in each of the files that
make up your [dithering] application.

Steps:

1. Open dither_matrix8.cpp.
	This is the main program for the algorithm.
2. All the header files and the source files have already been included in the project. 
3. Run the program and follow the instructions. 
4. Build the binary for the cpp using following commands:
    g++ -std=c++11 dither_matrix8.cpp
    ./a.out
5. output will be saved as dither_8.raw

CONSOLE APPLICATION : [Dithering_32] Project Overview
========================================================================

This file contains a summary of what you will find in each of the files that
make up your [dithering] application.

Steps:

1. Open dither_matrix32.cpp.
	This is the main program for the algorithm.
2. All the header files and the source files have already been included in the project. 
3. Run the program and follow the instructions. 
4. Build the binary for the cpp using following commands:
    g++ -std=c++11 dither_matrix32.cpp
    ./a.out
5. output will be saved as dither_32.raw





CONSOLE APPLICATION : [Error diffusion floyd] Project Overview
========================================================================

This file contains a summary of what you will find in each of the files that
make up your [error diffusion floyd] application.

Steps:

1. Open errordiffusionnew.cpp.
	This is the main program for the algorithm.
2. All the header files and the source files have already been included in the project. 
3. Run the program and follow the instructions. 
4. Build the binary for the cpp using following commands:
    g++ -std=c++11 errordiffusionnew.cpp
    ./a.out
5. output will be saved as err.raw


CONSOLE APPLICATION : [Error diffusion jarvis] Project Overview
========================================================================

This file contains a summary of what you will find in each of the files that
make up your [error diffusion jarvis] application.

Steps:

1. Open error_diff_jarvis.cpp.
	This is the main program for the algorithm.
2. All the header files and the source files have already been included in the project. 
3. Run the program and follow the instructions. 
4. Build the binary for the cpp using following commands:
    g++ -std=c++11 error_diff_jarvis.cpp
    ./a.out
5. output will be saved as err_jarvis.raw


CONSOLE APPLICATION : [Error diffusion stucki] Project Overview
========================================================================

This file contains a summary of what you will find in each of the files that
make up your [error diffusion stucki] application.

Steps:

1. Open error_diff_stucki.cpp.
	This is the main program for the algorithm.
2. All the header files and the source files have already been included in the project. 
3. Run the program and follow the instructions. 
4. Build the binary for the cpp using following commands:
    g++ -std=c++11 error_diff_stucki.cpp
    ./a.out
5. output will be saved as err_stucki.raw


CONSOLE APPLICATION : [color halftoning] Project Overview
========================================================================

This file contains a summary of what you will find in each of the files that
make up your [color halftoning] application.

Steps:

1. Open colorhalf.cpp.
	This is the main program for the algorithm.
2. All the header files and the source files have already been included in the project. 
3. Run the program and follow the instructions. 
4. Build the binary for the cpp using following commands:
    g++ -std=c++11 colorhalf.cpp
    ./a.out
5. output will be saved as colorhalf.raw



CONSOLE APPLICATION : [MBVQ] Project Overview
========================================================================

This file contains a summary of what you will find in each of the files that
make up your [MBVQ] application.

Steps:

1. Open MBVQ.cpp.
	This is the main program for the algorithm.
2. All the header files and the source files have already been included in the project. 
3. Run the program and follow the instructions. 
4. Build the binary for the cpp using following commands:
    g++ -std=c++11 MBVQ.cpp
    ./a.out
5. output will be saved as colorhalf.raw




   


